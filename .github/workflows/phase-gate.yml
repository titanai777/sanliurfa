name: Phase Gate

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  phase_check_tsconfig:
    name: phase:check:tsconfig
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci --legacy-peer-deps
      - name: Phase Tsconfig Check
        run: npm run phase:check:tsconfig

  phase_gate_ci:
    name: test:phase:gate:ci
    runs-on: ubuntu-latest
    needs: phase_check_tsconfig
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install
        run: npm ci --legacy-peer-deps
      - name: Ensure Rollup Linux Binary
        run: npm i -D @rollup/rollup-linux-x64-gnu --no-save --legacy-peer-deps
      - name: Run Phase Gate
        run: npm run phase:check:tsconfig && npm run lint:phase && npm run test:phase:latest && npm run build
